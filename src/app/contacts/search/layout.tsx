import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Αναζήτηση Επαφών",
};

export default function ContactSearchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
