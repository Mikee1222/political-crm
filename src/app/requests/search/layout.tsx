import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Αναζήτηση Αιτημάτων",
};

export default function RequestSearchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
